#include "{{name}}/{{name}}.h"
#include "log/log.h"

void {{name}}_Init(
    {%- if struct -%} {{name}}_{{Name}}* {{name}}
        {%- else -%} void {%- endif -%}
) {
    log_Debug("Initializing {{name}}...");
}

